apiVersion: v1
kind: Service
metadata:
  name: {{ include "kiali-istio-auth.fullname" . }}
  labels:
    {{- include "kiali-istio-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kiali-istio-auth.selectorLabels" . | nindent 4 }}
